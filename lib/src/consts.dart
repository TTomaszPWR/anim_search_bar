import 'package:flutter/material.dart';

const boxShadowBlack = [BoxShadow(
  color: Colors.black26,
  spreadRadius: -10.0,
  blurRadius: 10.0,
  offset: Offset(0.0, 10.0),
)];

const suffixIconBlack = Icon(
  Icons.close,
  size: 20.0,
  color: Colors.black,
);